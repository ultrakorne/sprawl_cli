# goreleaser config. Two binaries from one repo: prod (`sprawl`) and dev
# (`sprawl_dev`). Only the ldflag values differ. Keep this in sync with the
# Makefile.
#
# Still to wire up before distribution beyond GitHub releases:
#   - homebrew tap (see commented `brews:` block below)
#   - scoop bucket, if we target Windows

version: 2

project_name: sprawl

before:
  hooks:
    - go mod tidy

builds:
  - id: sprawl
    binary: sprawl
    main: ./cmd/sprawl
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags:
      - -s -w
      - -X github.com/ultrakorne/sprawl_cli/internal/build.APIURL=https://sprawl.today
      - -X github.com/ultrakorne/sprawl_cli/internal/build.AppName=sprawl
      - -X github.com/ultrakorne/sprawl_cli/internal/build.Version={{.Version}}
      - -X github.com/ultrakorne/sprawl_cli/internal/build.Commit={{.ShortCommit}}
      - -X github.com/ultrakorne/sprawl_cli/internal/build.Date={{.Date}}
    goos: [linux, darwin]
    goarch: [amd64, arm64]

  - id: sprawl_dev
    binary: sprawl_dev
    main: ./cmd/sprawl
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags:
      - -s -w
      - -X github.com/ultrakorne/sprawl_cli/internal/build.APIURL=http://localhost:4000
      - -X github.com/ultrakorne/sprawl_cli/internal/build.AppName=sprawl_dev
      - -X github.com/ultrakorne/sprawl_cli/internal/build.Version={{.Version}}
      - -X github.com/ultrakorne/sprawl_cli/internal/build.Commit={{.ShortCommit}}
      - -X github.com/ultrakorne/sprawl_cli/internal/build.Date={{.Date}}
    goos: [linux, darwin]
    goarch: [amd64, arm64]

archives:
  - id: sprawl
    ids: [sprawl]
    name_template: "sprawl_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]
  - id: sprawl_dev
    ids: [sprawl_dev]
    name_template: "sprawl_dev_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    formats: [tar.gz]

checksum:
  name_template: checksums.txt

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"

release:
  github:
    owner: ultrakorne
    name: sprawl_cli

# Homebrew distribution — uncomment once a tap exists.
# brews:
#   - repository:
#       owner: ultrakorne
#       name: homebrew-tap
#     homepage: https://github.com/ultrakorne/sprawl_cli
#     description: CLI for the sprawl API
